namespace entities
{
    public static class Schema
    {
        public const string SCHEMA_EMPRESA = nameof(empresa);
        public const string SCHEMA_SEGURANCA = nameof(seguranca);
        public const string SCHEMA_LOGS = nameof(logs);
    }
}
